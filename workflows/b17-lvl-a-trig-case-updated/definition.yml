# P-B17 · lvl-a · topic trig · role=case-updated-handler
# Case-trigger panel: ON (event_types=case_updated)
# Naming: docs/15-b17-product-test/01-naming-standard.md · DO-02-093 · COVERAGE-100
type: workflow
id: wf_1WpKJR8fJpE2vm4WhhjJPW
alias: b17-lvl-a-trig-case-updated
definition:
  title: "b17 · lvl-a · trig · case-updated · needs-trigger"
  description: >
    P-B17 product-test artifact. Level: lvl-a. Topic: T-TRIG. Role: case-updated handler.
    Case-trigger PANEL ON: event_types=[case_updated], tag_filters=[].
    Product emits case_updated when Case summary changes (UpdatedEventRead.field=summary).
    Workflow-side filter: only act when Case has topic-trig (get_case).
    On match: add tag trig-updated-handled + audit comment.
    RUN MODE: needs-trigger — bare Manual Run WILL FAIL without case_id.
    Org: B17 Lab · Workspace: b17-trig. Fire via REST PATCH summary (not WF actions).
  entrypoint:
    ref: normalize
    expects: {}
  actions:
    - ref: normalize
      action: core.script.run_python
      args:
        allow_network: false
        timeout_seconds: 30
        inputs:
          case_id: ${{ TRIGGER.case_id }}
          event_type: ${{ TRIGGER.event_type }}
          type: ${{ TRIGGER.type }}
          field: ${{ TRIGGER.field }}
          old: ${{ TRIGGER.old }}
          new: ${{ TRIGGER.new }}
        script: |
          def main(case_id, event_type, type, field, old, new):
              cid = str(case_id or "").strip()
              et = str(event_type or type or "case_updated")
              return {
                  "case_id": cid,
                  "event_type": et,
                  "field": str(field or ""),
                  "old": str(old or ""),
                  "new": str(new or ""),
              }
    - ref: require_case
      action: core.require
      depends_on: [normalize]
      args:
        conditions: ${{ FN.length(ACTIONS.normalize.result.case_id) > 0 }}
        require_all: true
        raise_error: true
    - ref: load_case
      action: core.cases.get_case
      depends_on: [require_case]
      args:
        case_id: ${{ ACTIONS.normalize.result.case_id }}
    - ref: decide
      action: core.script.run_python
      depends_on: [load_case]
      args:
        allow_network: false
        timeout_seconds: 30
        inputs:
          case: ${{ ACTIONS.load_case.result }}
          case_id: ${{ ACTIONS.normalize.result.case_id }}
          event_type: ${{ ACTIONS.normalize.result.event_type }}
          field: ${{ ACTIONS.normalize.result.field }}
          old: ${{ ACTIONS.normalize.result.old }}
          new: ${{ ACTIONS.normalize.result.new }}
        script: |
          def main(case, case_id, event_type, field, old, new):
              tags = []
              for t in (case or {}).get("tags") or []:
                  if isinstance(t, dict):
                      tags.append(str(t.get("ref") or t.get("name") or ""))
                  else:
                      tags.append(str(t))
              matched = "topic-trig" in tags
              return {
                  "matched_s": "yes" if matched else "no",
                  "tags": tags,
                  "case_id": case_id,
                  "event_type": event_type,
                  "audit": (
                      f"P-B17 T-TRIG case_updated MATCHED field={field or '?'} "
                      f"{old or '?'}→{new or '?'} tags={tags}"
                      if matched
                      else (
                          f"P-B17 T-TRIG case_updated SKIPPED (no topic-trig) "
                          f"field={field or '?'} tags={tags}"
                      )
                  ),
              }
    - ref: mark_handled
      action: core.cases.add_case_tag
      depends_on: [decide]
      run_if: ${{ FN.is_equal(ACTIONS.decide.result.matched_s, "yes") }}
      args:
        case_id: ${{ ACTIONS.normalize.result.case_id }}
        tag: trig-updated-handled
        create_if_missing: true
    - ref: audit
      action: core.cases.create_comment
      depends_on: [decide]
      args:
        case_id: ${{ ACTIONS.normalize.result.case_id }}
        content: ${{ ACTIONS.decide.result.audit }}
    - ref: pack
      action: core.transform.reshape
      depends_on: [audit]
      args:
        value:
          plan: P-B17
          level: lvl-a
          topic: trig
          role: case-updated-handler
          case_id: ${{ ACTIONS.normalize.result.case_id }}
          event_type: ${{ ACTIONS.normalize.result.event_type }}
          matched_s: ${{ ACTIONS.decide.result.matched_s }}
          handled: ${{ FN.is_equal(ACTIONS.decide.result.matched_s, "yes") }}
  returns: ${{ ACTIONS.pack.result }}
layout:
  trigger: { x: 0, y: 0 }
  actions:
    - { ref: normalize, x: 0, y: 120 }
    - { ref: require_case, x: 0, y: 240 }
    - { ref: load_case, x: 0, y: 360 }
    - { ref: decide, x: 0, y: 480 }
    - { ref: mark_handled, x: -120, y: 600 }
    - { ref: audit, x: 120, y: 600 }
    - { ref: pack, x: 120, y: 720 }
